spring:
  application:
    name: eureka-client
  cloud:
    config:
      profile: dev
      label: master
      uri: http://localhost:8764/
  datasource:
    url: mysql://localhost:3306/user
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
